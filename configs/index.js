import theme from './theme'
import navigation from './navigation'

export default {
  // theme configs
  theme,

  // navigation configs
  navigation,

  // pagination configs
  pagination: {
    itemsPerPage: 20
  },

  momentFormat: {
    default: 'LL',
    date: 'DD/MM/YYYY'
  }
}
